{
  "name": "forager",
  "version": "0.1.0",
  "private": true,
  "description": "Forager — Agentic grant operations platform",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "db:migrate": "supabase db push",
    "db:seed": "supabase db reset",
    "eval": "cd evals && promptfoo eval"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.22.4",
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
